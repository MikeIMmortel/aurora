import type { Measurement } from '../types/measurement';

/**
 * Initiële metingen waarmee een nieuwe installatie start.
 * Leeg gelaten zodat persoonlijke data niet in het (publieke) repo staat —
 * importeer je eigen PDF via /import om te beginnen.
 */
export const SEED_MEASUREMENTS: Measurement[] = [];
